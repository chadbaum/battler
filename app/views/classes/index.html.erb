<ol class="breadcrumb" style="margin-bottom: 5px;">
  <li class="active">Classes</li>
</ol>

<% @classes.each do |job| %>
  <%= image_tag "/assets/icons/#{job.alias}.png", class: 'class-icon' %> <%= link_to job.name, show_class_path(job) %><br>
<% end %>
